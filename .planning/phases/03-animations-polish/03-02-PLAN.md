---
phase: 03-animations-polish
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/terminal.tsx
  - src/components/footer.tsx
  - src/app/[locale]/layout.tsx
  - messages/de.json
  - messages/en.json
autonomous: true
requirements: [TECH-03]

must_haves:
  truths:
    - "User can open terminal overlay via Ctrl+` keyboard shortcut"
    - "User can open terminal via clicking a hint in the footer"
    - "Terminal responds to whoami, skills, projects, motivation, contact commands"
    - "Terminal responds to help (lists commands) and clear (clears output)"
    - "Terminal responds to unknown commands with an error message"
    - "Terminal can be closed with exit command or Escape key"
    - "Terminal works in both DE and EN based on current locale"
  artifacts:
    - path: "src/components/terminal.tsx"
      provides: "Interactive CLI terminal overlay with TerminalHint"
      exports: ["Terminal", "TerminalHint"]
    - path: "messages/de.json"
      provides: "Terminal namespace with DE responses for all commands"
      contains: "Terminal"
    - path: "messages/en.json"
      provides: "Terminal namespace with EN responses for all commands"
      contains: "Terminal"
  key_links:
    - from: "src/components/terminal.tsx"
      to: "messages/de.json"
      via: "useTranslations('Terminal')"
      pattern: "useTranslations.*Terminal"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/terminal.tsx"
      via: "Terminal rendered as fixed overlay in layout"
      pattern: "<Terminal"
    - from: "src/components/footer.tsx"
      to: "src/components/terminal.tsx"
      via: "TerminalHint dispatches custom event to toggle terminal"
      pattern: "TerminalHint"
---

<objective>
Create a Terminal Easter Egg -- a hidden CLI-style overlay that responds to 5+ commands (whoami, skills, projects, motivation, contact) with i18n responses in DE/EN, triggered by keyboard shortcut or footer hint.

Purpose: Fulfill TECH-03 (Terminal Easter Egg with 3-5 commands)
Output: Working Terminal component, i18n messages, footer hint, wired into layout
</objective>

<execution_context>
@/Users/tobiasludwig/Dev/tjl/bitmarck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tobiasludwig/Dev/tjl/bitmarck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-animations-polish/03-RESEARCH.md
@.planning/phases/03-animations-polish/03-PATTERNS.md
@.planning/phases/03-animations-polish/03-01-SUMMARY.md

<interfaces>
<!-- Layout after Plan 01 (MotionProvider already added) -->
From src/app/[locale]/layout.tsx (post Plan 01):
```typescript
import {MotionProvider} from '@/components/motion-provider'
// ...existing imports...

<NextIntlClientProvider messages={messages}>
  <MotionProvider>
    <StickyHeader />
    {children}
    <Footer />
  </MotionProvider>
</NextIntlClientProvider>
```

<!-- Footer (server component) -->
From src/components/footer.tsx:
```typescript
import {getTranslations} from 'next-intl/server'

export async function Footer() {
  const t = await getTranslations('Footer')
  return (
    <footer className="border-t border-border bg-surface-subtle py-2xl px-md md:px-xl">
      <div className="mx-auto max-w-6xl text-center">
        <p className="text-label text-text-secondary">{t('builtWith')}</p>
      </div>
    </footer>
  )
}
```

<!-- Existing i18n namespaces -->
Namespaces: Metadata, NotFound, Nav, Hero, Metrics, Career, Projects, ProjectDetail, Skills, FAQ, Footer, ProjectData

<!-- StickyHeader analog for Terminal (client component with useEffect + event listeners) -->
From src/components/sticky-header.tsx:
```typescript
'use client'
import {useEffect, useState} from 'react'
import {useTranslations} from 'next-intl'
const SECTION_IDS = ['career', 'projects', 'skills', 'faq'] as const
// useEffect with event listener + cleanup return pattern
```

<!-- Project conventions -->
- 'use client' on line 1 for client components
- Named exports only (no export default)
- No semicolons after imports
- useTranslations('Namespace') for client-side i18n
- getTranslations('Namespace') for server-side i18n
- Tailwind tokens: bg-surface-raised, text-text-primary, text-text-secondary, border-border
- Constants as typed tuples: const X = [...] as const
- StickyHeader uses z-50, Terminal overlay should use z-[60] to appear above
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Terminal i18n messages and create Terminal component</name>
  <files>messages/de.json, messages/en.json, src/components/terminal.tsx</files>
  <read_first>
    - messages/de.json (full file -- namespace structure and existing keys)
    - messages/en.json (full file -- parallel structure)
    - src/components/sticky-header.tsx (client component with useEffect + event listeners + useTranslations)
    - src/components/faq-section.tsx (client component conventions)
  </read_first>
  <action>
1. Add "Terminal" namespace to `messages/de.json`:
```json
"Terminal": {
  "greeting": "tobias@bitmarck:~$ Willkommen! Tippe 'help' fuer eine Liste der Befehle.",
  "whoami": "Tobias Ludwig -- Business Analyst IAM Kandidat bei Bitmarck. 10+ Jahre IT-Erfahrung, vom Systemadministrator zum Business Analysten. Ich uebersetze zwischen Technik und Fachbereich.",
  "skills": "IAM: Keycloak, OAuth2/OIDC, SAML, RBAC/ABAC, Active Directory\nBusiness Analyse: Stakeholder-Interviews, BPMN, User Stories, Anforderungsdokumentation\nDevOps: Docker, Kubernetes, CI/CD, Linux, Monitoring\nSprachen: Deutsch (Muttersprache), Englisch (verhandlungssicher)",
  "projects": "1. Auth API Service -- Keycloak-basierter Authentifizierungsdienst mit OIDC/SAML\n2. Next CMS -- Headless CMS mit Next.js und rollenbasiertem Zugang\n3. Server-Cluster -- Kubernetes-Cluster mit automatisiertem Deployment",
  "motivation": "Bitmarck verbindet Gesundheits-IT mit modernen Technologien. Als Business Analyst IAM kann ich meine technische Expertise in Identity & Access Management mit analytischem Denken verbinden -- genau die Schnittstelle, die mich antreibt.",
  "contact": "Email: tobias@ludwigdev.de\nWebsite: ludwigdev.de\nGitHub: github.com/tobiasludwig",
  "help": "Verfuegbare Befehle:\n  whoami      -- Wer ist Tobias?\n  skills      -- Technische Skills\n  projects    -- Projekt-Highlights\n  motivation  -- Warum Bitmarck?\n  contact     -- Kontaktdaten\n  help        -- Diese Hilfe\n  clear       -- Terminal leeren\n  exit        -- Terminal schliessen",
  "unknown": "Befehl nicht gefunden: {cmd}. Tippe 'help' fuer eine Liste der Befehle.",
  "prompt": "tobias@bitmarck:~$"
}
```

2. Add parallel "Terminal" namespace to `messages/en.json`:
```json
"Terminal": {
  "greeting": "tobias@bitmarck:~$ Welcome! Type 'help' for a list of commands.",
  "whoami": "Tobias Ludwig -- Business Analyst IAM candidate at Bitmarck. 10+ years IT experience, from system administrator to business analyst. I translate between technology and business.",
  "skills": "IAM: Keycloak, OAuth2/OIDC, SAML, RBAC/ABAC, Active Directory\nBusiness Analysis: Stakeholder interviews, BPMN, User Stories, Requirements documentation\nDevOps: Docker, Kubernetes, CI/CD, Linux, Monitoring\nLanguages: German (native), English (fluent)",
  "projects": "1. Auth API Service -- Keycloak-based authentication service with OIDC/SAML\n2. Next CMS -- Headless CMS with Next.js and role-based access\n3. Server-Cluster -- Kubernetes cluster with automated deployment",
  "motivation": "Bitmarck combines health IT with modern technologies. As a Business Analyst IAM, I can merge my technical IAM expertise with analytical thinking -- exactly the intersection that drives me.",
  "contact": "Email: tobias@ludwigdev.de\nWebsite: ludwigdev.de\nGitHub: github.com/tobiasludwig",
  "help": "Available commands:\n  whoami      -- Who is Tobias?\n  skills      -- Technical skills\n  projects    -- Project highlights\n  motivation  -- Why Bitmarck?\n  contact     -- Contact info\n  help        -- Show this help\n  clear       -- Clear terminal\n  exit        -- Close terminal",
  "unknown": "Command not found: {cmd}. Type 'help' for a list of commands.",
  "prompt": "tobias@bitmarck:~$"
}
```

3. Add `terminalHint` key to the existing "Footer" namespace in both files:
   - DE: `"terminalHint": "Druecke Ctrl+\` fuer eine Ueberraschung"`
   - EN: `"terminalHint": "Press Ctrl+\` for a surprise"`

4. Create `src/components/terminal.tsx` with TWO named exports:

**`Terminal` component** (the overlay):
- State: `isOpen` (boolean), `lines` (array of `{type: 'input' | 'output', text: string}`), `input` (string)
- `useRef<HTMLInputElement>` for auto-focus, `useRef<HTMLDivElement>` for scroll-to-bottom
- `useTranslations('Terminal')` for i18n responses
- `useCallback` for `handleCommand`

- Keyboard shortcut (useEffect): `Ctrl+\`` toggles open/close, `Escape` closes. Clean up on unmount.
- Custom event listener (useEffect): listen for `'toggle-terminal'` DOM event to set isOpen true. This allows TerminalHint (in footer) to trigger opening without shared state.
- Auto-focus input when opened (useEffect watching isOpen).
- On open: show greeting line as initial output.
- On submit (Enter key in input): add input line `> {cmd}`, process command, add output line, clear input, scroll to bottom.

- Command handling:
  - `const COMMANDS = ['whoami', 'skills', 'projects', 'motivation', 'contact', 'help'] as const`
  - If command is in COMMANDS: `t(command)` for response
  - If `clear`: reset lines to empty
  - If `exit`: close terminal, reset lines
  - Otherwise: `t('unknown', {cmd: trimmedInput})`

- Visual styling:
  - Backdrop: `fixed inset-0 z-[60] flex items-end justify-center p-md md:p-xl bg-black/60`
  - Click backdrop to close (onClick on backdrop div, stopPropagation on terminal window)
  - Terminal window: `bg-[#1a1a2e] rounded-lg border border-border max-w-2xl w-full max-h-[60vh] flex flex-col`
  - Title bar: `flex items-center gap-xs px-md py-sm border-b border-border/50` with three decorative dots (red `bg-red-500`, yellow `bg-yellow-500`, green `bg-green-500`, each `h-3 w-3 rounded-full`) and title text `tobias@bitmarck` in `text-text-secondary text-sm font-mono ml-sm`
  - Output area: `flex-1 overflow-y-auto p-md font-mono text-sm` with auto-scroll-to-bottom ref. Input lines `text-green-400`, output lines `text-green-300/80`. Use `whitespace-pre-wrap` on output text to render `\n`.
  - Input row: `flex items-center gap-sm px-md py-sm border-t border-border/50 font-mono text-sm`
  - Prompt: `text-green-400 shrink-0` showing `t('prompt')`
  - Input field: `<input>` with `bg-transparent border-none outline-none flex-1 text-green-400 font-mono text-sm caret-green-400`

- AnimatePresence + motion.div for enter/exit: `initial={{opacity: 0, y: 20}}`, `animate={{opacity: 1, y: 0}}`, `exit={{opacity: 0, y: 20}}`, `transition={{duration: 0.2}}`. Import `AnimatePresence, motion` from `motion/react`.

**`TerminalHint` component** (for footer):
- A small client component that renders a clickable `<button>` styled as subtle text.
- Uses `useTranslations('Footer')` to get `t('terminalHint')`.
- On click: dispatches `new CustomEvent('toggle-terminal')` on `window`.
- Styling: `mt-sm text-xs text-text-secondary/60 hover:text-text-secondary transition-colors cursor-pointer` -- very subtle, discoverable but not distracting.
  </action>
  <verify>
    <automated>cd /Users/tobiasludwig/Dev/tjl/bitmarck && node -e "const de = require('./messages/de.json'); const en = require('./messages/en.json'); if (!de.Terminal || !en.Terminal) throw new Error('Missing Terminal namespace'); if (!de.Footer.terminalHint || !en.Footer.terminalHint) throw new Error('Missing terminalHint'); console.log('i18n OK')" && test -f src/components/terminal.tsx && echo "Files OK"</automated>
  </verify>
  <acceptance_criteria>
    - messages/de.json has Terminal namespace with keys: greeting, whoami, skills, projects, motivation, contact, help, unknown, prompt
    - messages/en.json has matching Terminal namespace with English translations
    - Both message files have Footer.terminalHint key
    - src/components/terminal.tsx exports Terminal and TerminalHint
    - Terminal uses 'use client', useTranslations('Terminal'), AnimatePresence for enter/exit
    - Terminal responds to whoami, skills, projects, motivation, contact, help, clear, exit
    - Unknown commands show error with command name via t('unknown', {cmd})
    - TerminalHint dispatches 'toggle-terminal' custom event on click
  </acceptance_criteria>
  <done>Terminal component created with full command handling, i18n in DE/EN, TerminalHint for footer trigger</done>
</task>

<task type="auto">
  <name>Task 2: Wire Terminal into layout and TerminalHint into Footer</name>
  <files>src/app/[locale]/layout.tsx, src/components/footer.tsx</files>
  <read_first>
    - src/app/[locale]/layout.tsx (read current state after Plan 01 added MotionProvider)
    - src/components/footer.tsx (current footer)
    - src/components/terminal.tsx (just created -- verify Terminal and TerminalHint exports exist)
  </read_first>
  <action>
1. Modify `src/app/[locale]/layout.tsx`:
   - Add import: `import {Terminal} from '@/components/terminal'`
   - Add `<Terminal />` inside `<MotionProvider>`, after `<Footer />`:
   ```
   <MotionProvider>
     <StickyHeader />
     {children}
     <Footer />
     <Terminal />
   </MotionProvider>
   ```

2. Modify `src/components/footer.tsx`:
   - Add import: `import {TerminalHint} from '@/components/terminal'`
   - Add `<TerminalHint />` below the builtWith paragraph. TerminalHint is a client component rendered as a child of a server component -- standard Next.js pattern.
   ```typescript
   <div className="mx-auto max-w-6xl text-center">
     <p className="text-label text-text-secondary">
       {t('builtWith')}
     </p>
     <TerminalHint />
   </div>
   ```
  </action>
  <verify>
    <automated>cd /Users/tobiasludwig/Dev/tjl/bitmarck && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx imports Terminal and renders it after Footer inside MotionProvider
    - footer.tsx imports TerminalHint and renders it below builtWith text
    - next build succeeds without errors
    - No hydration mismatch errors
  </acceptance_criteria>
  <done>Terminal Easter Egg fully wired -- opens via Ctrl+` or footer hint click, responds to all commands in DE/EN, animates in/out</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keyboard input -> Terminal command parser | User-typed commands parsed and matched against allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | T (Tampering) | terminal.tsx command input | mitigate | Commands matched against const COMMANDS tuple allowlist; unknown input returns generic error; no eval, no dynamic execution; input is `.trim().toLowerCase()` only |
| T-03-04 | I (Information Disclosure) | Terminal responses | accept | All responses are public marketing content (same as visible on page); no secrets, no server data |
| T-03-05 | D (Denial of Service) | terminal.tsx lines state | accept | Lines array grows with usage but session-only; clear/exit resets; no persistence; no server calls |
</threat_model>

<verification>
1. `next build` completes without errors
2. Terminal opens with Ctrl+` keyboard shortcut
3. Terminal opens when clicking footer hint
4. Commands whoami, skills, projects, motivation, contact return appropriate DE/EN responses
5. help command lists all available commands
6. clear command empties the terminal
7. exit command and Escape key close the terminal
8. Unknown command shows error message with the command name
9. Terminal has enter/exit animation (slide up/fade in)
</verification>

<success_criteria>
- Terminal Easter Egg fully functional with 7 commands (whoami, skills, projects, motivation, contact, help, clear) plus exit
- Responses bilingual (DE/EN) via next-intl
- Discoverable via subtle footer hint
- Keyboard-accessible (Ctrl+`, Escape)
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-animations-polish/03-02-SUMMARY.md`
</output>
