---
phase: 01-foundation
plan: 02
subsystem: infra
tags: [docker, standalone, deployment, node22]

# Dependency graph
requires:
  - phase: 01-01
    provides: "Next.js 16 scaffold with standalone output config and i18n routing"
provides:
  - "3-stage Dockerfile for standalone Next.js deployment"
  - ".dockerignore excluding sensitive/unnecessary files from build context"
  - "Verified container serving /de and /en on port 3000 as non-root user"
affects: [02-core-content, 03-interactive, 04-polish]

# Tech tracking
tech-stack:
  added: []
  patterns: [3-stage-docker-build, non-root-container]

key-files:
  created:
    - Dockerfile
    - .dockerignore
  modified:
    - package-lock.json

key-decisions:
  - "Regenerated package-lock.json to fix @swc/helpers version conflict between npm 11 (host) and npm 10 (container)"

patterns-established:
  - "Docker build: 3-stage (dependencies, builder, runner) with node:22-slim base"
  - "Container runs as non-root 'node' user with HOSTNAME=0.0.0.0"
  - ".dockerignore excludes .planning/ to keep planning artifacts out of images"

requirements-completed: [TECH-02]

# Metrics
duration: 4min
completed: 2026-04-19
---

# Phase 1 Plan 2: Docker Standalone Deployment Summary

**3-stage Dockerfile with node:22-slim serving standalone Next.js on port 3000 as non-root user**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-19T15:46:04Z
- **Completed:** 2026-04-19T15:50:05Z
- **Tasks:** 1
- **Files modified:** 3

## Accomplishments
- 3-stage Docker build (dependencies, builder, runner) using node:22-slim
- Container serves /de and /en with HTTP 200
- Runs as non-root `node` user (STRIDE T-01-03 mitigated)
- .dockerignore excludes .git, .next, node_modules, .planning (T-01-04 mitigated)

## Task Commits

Each task was committed atomically:

1. **Task 1: Create Dockerfile and .dockerignore for standalone build** - `7b0db22` (feat)

## Files Created/Modified
- `Dockerfile` - 3-stage standalone build: dependencies (npm ci), builder (next build), runner (node server.js)
- `.dockerignore` - Excludes node_modules, .next, .git, .planning from Docker context
- `package-lock.json` - Regenerated to fix @swc/helpers version sync for container npm

## Decisions Made
- Regenerated package-lock.json (rm + npm install) to resolve @swc/helpers@0.5.21 missing from lock file, which caused npm ci to fail in the container's npm 10 environment

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed package-lock.json sync for container npm compatibility**
- **Found during:** Task 1 (Docker build)
- **Issue:** `npm ci` in container (npm 10.9.7) failed with "Missing: @swc/helpers@0.5.21 from lock file" -- the lock file generated by local npm 11 had a deduplication that npm 10 couldn't resolve
- **Fix:** Removed node_modules and package-lock.json, ran `npm install` to regenerate a clean lock file
- **Files modified:** package-lock.json
- **Verification:** docker build exits 0, npm ci succeeds in container
- **Committed in:** 7b0db22 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Lock file regeneration was necessary for Docker build to succeed. No scope creep.

## Issues Encountered
- Docker Desktop was not running at first; started via `open -a Docker` and waited for daemon readiness before proceeding.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Docker infrastructure complete -- any future content changes will build and deploy via `docker build`
- Foundation phase (01) fully complete: Next.js 16 + i18n + design tokens + Docker
- Ready for Phase 2: Core Content

---
*Phase: 01-foundation*
*Completed: 2026-04-19*
