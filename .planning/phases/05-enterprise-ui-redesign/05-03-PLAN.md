---
phase: 05-enterprise-ui-redesign
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/sticky-header.tsx
  - src/components/footer.tsx
  - src/components/career-section.tsx
  - src/components/projects-section.tsx
  - src/components/skills-section.tsx
  - src/components/faq-section.tsx
autonomous: false
requirements:
  - UI-REDESIGN

must_haves:
  truths:
    - "Header has backdrop blur effect with semi-transparent background"
    - "Footer has dark navy background matching the hero bookend pattern"
    - "Content sections alternate between white and light blue-gray backgrounds"
    - "Section headings use the larger text-section size with accent underline"
    - "Mobile and desktop have consistent, high-quality design"
  artifacts:
    - path: "src/components/sticky-header.tsx"
      provides: "Semi-transparent header with backdrop blur"
      contains: "backdrop-blur"
    - path: "src/components/footer.tsx"
      provides: "Dark navy footer with white text"
      contains: "primary-900"
    - path: "src/components/career-section.tsx"
      provides: "Section with text-section heading and accent underline"
      contains: "text-section"
    - path: "src/components/projects-section.tsx"
      provides: "Alternating background section"
      contains: "bg-primary-50"
    - path: "src/components/skills-section.tsx"
      provides: "Section heading with accent underline"
      contains: "text-section"
    - path: "src/components/faq-section.tsx"
      provides: "Alternating background section with updated heading"
      contains: "text-section"
  key_links:
    - from: "src/app/globals.css"
      to: "src/components/footer.tsx"
      via: "primary-900 color token"
      pattern: "primary-900"
    - from: "src/components/sticky-header.tsx"
      to: "hero-section dark bg"
      via: "backdrop-blur creates smooth transition over dark hero"
      pattern: "backdrop-blur"
---

<objective>
Complete the visual polish: semi-transparent header with blur, dark navy footer bookend, alternating section backgrounds for rhythm, and upgraded section headings with accent underlines.

Purpose: The header, footer, and section backgrounds create the "frame" around the content. A blurred header feels modern over the dark hero. A dark footer creates a visual bookend. Alternating backgrounds separate content sections. Larger headings with accent marks establish hierarchy.
Output: Updated header, footer, and all 4 content section components
</objective>

<execution_context>
@/Users/tobiasludwig/Dev/tjl/bitmarck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tobiasludwig/Dev/tjl/bitmarck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-enterprise-ui-redesign/05-RESEARCH.md
@.planning/phases/05-enterprise-ui-redesign/05-01-SUMMARY.md

<interfaces>
<!-- Design tokens from Plan 01 (globals.css @theme) -->
Available colors: primary-900 (#0F172A), primary-800 (#1E293B), primary-700 (#334155), primary-50 (#F8FAFC)
Available text sizes: text-section (clamp(24px, 3vw, 32px))

From src/components/sticky-header.tsx:
```tsx
'use client' component with scroll-spy IntersectionObserver
— Header: <header className="sticky top-0 z-50 h-[64px] border-b border-border bg-surface-raised">
— IMPORTANT: Do NOT change h-[64px] or z-50 — animations depend on 64px header height
```

From src/components/footer.tsx:
```tsx
Server component with getTranslations('Footer')
— <footer className="border-t border-border bg-surface-subtle py-2xl px-md md:px-xl">
— <p className="text-label text-text-secondary">
```

Section order in page.tsx: Hero > Career > Projects > Skills > FAQ
— Career = no bg class (inherits body bg-surface)
— Projects = no bg class
— Skills = no bg class  
— FAQ = no bg class
Alternating pattern: Career (white/default) > Projects (primary-50) > Skills (white/default) > FAQ (primary-50)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Semi-transparent header with backdrop blur</name>
  <files>src/components/sticky-header.tsx</files>
  <read_first>src/components/sticky-header.tsx</read_first>
  <action>
Replace the `<header>` className from:
```
sticky top-0 z-50 h-[64px] border-b border-border bg-surface-raised
```
to:
```
sticky top-0 z-50 h-[64px] border-b border-border/50 bg-surface-raised/80 backdrop-blur-md
```
Changes: `border-border` to `border-border/50` (subtler), `bg-surface-raised` to `bg-surface-raised/80` (semi-transparent white), add `backdrop-blur-md`.

CRITICAL: Do NOT change `h-[64px]` or `z-50` — the AnimateOnScroll rootMargin depends on the 64px header height and the Terminal overlay uses z-[60] above z-50.

Do NOT change any scroll-spy logic, IntersectionObserver, nav items, or event handlers.
  </action>
  <verify>
    <automated>grep -q "backdrop-blur-md" src/components/sticky-header.tsx && grep -q "h-\[64px\]" src/components/sticky-header.tsx && grep -q "z-50" src/components/sticky-header.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>Header has backdrop-blur-md, bg-surface-raised/80, border-border/50. Height remains h-[64px], z-index remains z-50.</acceptance_criteria>
  <done>Header is semi-transparent with backdrop blur, maintains 64px height and z-50 stacking</done>
</task>

<task type="auto">
  <name>Task 2: Dark footer, alternating section backgrounds, section heading upgrades</name>
  <files>src/components/footer.tsx, src/components/career-section.tsx, src/components/projects-section.tsx, src/components/skills-section.tsx, src/components/faq-section.tsx</files>
  <read_first>src/components/footer.tsx, src/components/career-section.tsx, src/components/projects-section.tsx, src/components/skills-section.tsx, src/components/faq-section.tsx</read_first>
  <action>
**footer.tsx:**

Replace the `<footer>` className from:
```
border-t border-border bg-surface-subtle py-2xl px-md md:px-xl
```
to:
```
border-t border-primary-800 bg-primary-900 py-2xl px-md md:px-xl
```

Replace the `<p>` className from:
```
text-label text-text-secondary
```
to:
```
text-label text-white/60
```

**career-section.tsx — section heading upgrade + accent underline:**

Replace the `<h2>` from:
```tsx
<h2 id="career-heading" className="text-heading font-semibold text-text-primary">
  {t('heading')}
</h2>
```
to:
```tsx
<h2 id="career-heading" className="text-section font-semibold text-text-primary inline-block">
  {t('heading')}
  <span className="block h-1 w-12 bg-accent rounded-full mt-sm" aria-hidden="true" />
</h2>
```
Changes: `text-heading` to `text-section`, add `inline-block`, add accent underline span.

Career section is the 1st content section — keeps default background (no bg class change needed).

**projects-section.tsx — alternating bg + heading upgrade:**

Add `bg-primary-50` to the `<section>` className. Change from:
```
py-3xl px-md md:px-xl scroll-mt-[64px]
```
to:
```
py-3xl px-md md:px-xl scroll-mt-[64px] bg-primary-50
```

Replace the `<h2>` from:
```tsx
<h2 id="projects-heading" className="text-heading font-semibold text-text-primary mb-xl">
  {t('heading')}
</h2>
```
to:
```tsx
<h2 id="projects-heading" className="text-section font-semibold text-text-primary mb-xl inline-block">
  {t('heading')}
  <span className="block h-1 w-12 bg-accent rounded-full mt-sm" aria-hidden="true" />
</h2>
```

**skills-section.tsx — heading upgrade (default bg, 3rd section):**

Replace the `<h2>` from:
```tsx
<h2 id="skills-heading" className="text-heading font-semibold text-text-primary mb-xl">
  {t('heading')}
</h2>
```
to:
```tsx
<h2 id="skills-heading" className="text-section font-semibold text-text-primary mb-xl inline-block">
  {t('heading')}
  <span className="block h-1 w-12 bg-accent rounded-full mt-sm" aria-hidden="true" />
</h2>
```

Skills section keeps default background (3rd section = white).

**faq-section.tsx — alternating bg + heading upgrade:**

Add `bg-primary-50` to the `<section>` className. Change from:
```
py-3xl px-md md:px-xl scroll-mt-[64px]
```
to:
```
py-3xl px-md md:px-xl scroll-mt-[64px] bg-primary-50
```

Replace the `<h2>` from:
```tsx
<h2 id="faq-heading" className="text-heading font-semibold text-text-primary mb-xl">
  {t('heading')}
</h2>
```
to:
```tsx
<h2 id="faq-heading" className="text-section font-semibold text-text-primary mb-xl inline-block">
  {t('heading')}
  <span className="block h-1 w-12 bg-accent rounded-full mt-sm" aria-hidden="true" />
</h2>
```

Do NOT change any component logic, imports, translation keys, or data structures.
  </action>
  <verify>
    <automated>grep -q "bg-primary-900" src/components/footer.tsx && grep -q "text-section" src/components/career-section.tsx && grep -q "bg-primary-50" src/components/projects-section.tsx && grep -q "bg-primary-50" src/components/faq-section.tsx && grep -q "bg-accent rounded-full" src/components/skills-section.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>Footer uses bg-primary-900 with text-white/60. Projects and FAQ sections have bg-primary-50. All four section headings use text-section with accent underline span. Career and Skills keep default (white) background.</acceptance_criteria>
  <done>Footer is dark navy bookend. Sections alternate white/primary-50. All section headings are larger (text-section) with blue accent underline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete enterprise UI redesign across all plans: dark hero gradient, glass metric cards, accent timeline, elevated project cards, accent tags, blurred header, dark footer, alternating section backgrounds, upgraded section headings</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/de
    2. Check hero: Dark navy gradient background, white text, glass metric cards, radial glow, active CTA button
    3. Scroll past hero: Header should show backdrop blur (slightly transparent over content)
    4. Career section: Timeline has blue accent line and dots with ring effect, heading has accent underline
    5. Projects section: Light blue-gray background (bg-primary-50), cards have shadow depth and lift on hover, tags are blue-tinted
    6. Skills section: White background, heading has accent underline, tags are blue-tinted
    7. FAQ section: Light blue-gray background, heading has accent underline
    8. Footer: Dark navy background with white text
    9. Test on mobile width (375px): Verify all sections render correctly
    10. Overall impression: Does it look like a professional enterprise product, not a generic template?
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific visual issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — purely visual CSS class changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | I (Information Disclosure) | footer.tsx | accept | No data changes — color/styling only |
</threat_model>

<verification>
1. `npm run build` completes without errors
2. Visual: Header blurs content scrolling behind it
3. Visual: Footer is dark navy matching hero palette
4. Visual: Projects and FAQ have light blue-gray background; Career and Skills are white
5. Visual: All section headings are larger with accent underline
6. Visual: Mobile layout is consistent with desktop
</verification>

<success_criteria>
- Header has backdrop-blur-md and semi-transparent background
- Footer uses primary-900 background with white/60 text
- Projects and FAQ sections have bg-primary-50
- Career and Skills sections keep default white background
- All four section headings use text-section with accent underline span
- 64px header height and z-50 unchanged
- Overall design feels professional and enterprise-quality
</success_criteria>

<output>
After completion, create `.planning/phases/05-enterprise-ui-redesign/05-03-SUMMARY.md`
</output>
