---
phase: 05-enterprise-ui-redesign
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/career-timeline.tsx
  - src/components/project-card.tsx
  - src/components/tag.tsx
autonomous: true
requirements:
  - UI-REDESIGN

must_haves:
  truths:
    - "Career timeline has accent-colored line and dots instead of gray"
    - "Project cards have layered shadows at rest and elevated shadows on hover"
    - "Project cards lift slightly on hover (translate-y)"
    - "Tags use accent color tint instead of neutral gray"
    - "Timeline dots have accent ring effect for visual emphasis"
  artifacts:
    - path: "src/components/career-timeline.tsx"
      provides: "Accent-colored timeline with ring dots"
      contains: "bg-accent"
    - path: "src/components/project-card.tsx"
      provides: "Elevated project cards with shadow depth"
      contains: "shadow-card"
    - path: "src/components/tag.tsx"
      provides: "Accent-tinted tag pills"
      contains: "accent/5"
  key_links:
    - from: "src/app/globals.css"
      to: "src/components/project-card.tsx"
      via: "shadow-card and shadow-card-hover tokens"
      pattern: "shadow-card"
---

<objective>
Add visual depth to the content sections: accent-colored career timeline, elevated project cards with hover effects, and accent-tinted skill tags.

Purpose: The content sections currently look flat with gray borders and no shadows. Adding layered shadows, accent colors on interactive elements, and hover elevation transforms these from "basic HTML" to "professional product UI."
Output: Updated career-timeline.tsx, project-card.tsx, and tag.tsx with enterprise visual depth
</objective>

<execution_context>
@/Users/tobiasludwig/Dev/tjl/bitmarck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tobiasludwig/Dev/tjl/bitmarck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-enterprise-ui-redesign/05-RESEARCH.md
@.planning/phases/05-enterprise-ui-redesign/05-01-SUMMARY.md

<interfaces>
<!-- Design tokens from Plan 01 (globals.css @theme) -->
Available shadow utilities: shadow-card, shadow-card-hover, shadow-elevated
Available colors: primary-900, primary-800, primary-700, primary-50, accent-light
Available text sizes: text-hero, text-subhero, text-section

From src/components/career-timeline.tsx:
```tsx
export async function CareerTimeline() — server component
— Timeline line: <div className="absolute left-[3px] top-[4px] bottom-[4px] w-px bg-border">
— Timeline dot: <div className="absolute -left-lg top-[6px] h-2 w-2 rounded-full border border-border bg-surface-raised">
— Station items in <ol> with <li className="relative pl-lg">
```

From src/components/project-card.tsx:
```tsx
export async function ProjectCard({project}) — server component
— Card wrapper: <article className="rounded-lg border border-border bg-surface-raised p-lg flex flex-col gap-md hover:shadow-sm transition-shadow duration-150 ease">
— Uses <Tag>, <Link>, getRepoData for GitHub data
```

From src/components/tag.tsx:
```tsx
export function Tag({children}) — server component
— <span className="inline-flex items-center rounded-full border border-border bg-surface-subtle px-md py-sm text-label text-text-secondary">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Accent-colored career timeline with ring dots</name>
  <files>src/components/career-timeline.tsx</files>
  <read_first>src/components/career-timeline.tsx</read_first>
  <action>
**Timeline vertical line** — replace the className from:
```
absolute left-[3px] top-[4px] bottom-[4px] w-px bg-border
```
to:
```
absolute left-[3px] top-[4px] bottom-[4px] w-0.5 bg-linear-to-b from-accent to-accent/30
```
Changes: `w-px` to `w-0.5` (slightly thicker), `bg-border` to gradient from solid accent to faded accent.

**Timeline dots** — replace the className from:
```
absolute -left-lg top-[6px] h-2 w-2 rounded-full border border-border bg-surface-raised
```
to:
```
absolute -left-lg top-[6px] h-3 w-3 rounded-full bg-accent ring-4 ring-accent/10
```
Changes: `h-2 w-2` to `h-3 w-3` (slightly larger), remove border, add filled `bg-accent` with subtle ring effect.

Do NOT change any text content, translation keys, component structure, or the STATION_KEYS array.
  </action>
  <verify>
    <automated>grep -q "bg-linear-to-b from-accent to-accent/30" src/components/career-timeline.tsx && grep -q "ring-4 ring-accent/10" src/components/career-timeline.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>Timeline line uses accent gradient (bg-linear-to-b from-accent to-accent/30) with w-0.5 width. Dots are h-3 w-3, bg-accent filled, with ring-4 ring-accent/10 glow effect.</acceptance_criteria>
  <done>Career timeline has accent-colored gradient line and filled accent dots with ring effect</done>
</task>

<task type="auto">
  <name>Task 2: Elevated project cards and accent-tinted tags</name>
  <files>src/components/project-card.tsx, src/components/tag.tsx</files>
  <read_first>src/components/project-card.tsx, src/components/tag.tsx</read_first>
  <action>
**project-card.tsx** — replace the `<article>` className from:
```
rounded-lg border border-border bg-surface-raised p-lg flex flex-col gap-md hover:shadow-sm transition-shadow duration-150 ease
```
to:
```
rounded-xl border border-border/50 bg-surface-raised p-lg flex flex-col gap-md shadow-card hover:shadow-card-hover hover:-translate-y-0.5 transition-all duration-200
```
Changes: `rounded-lg` to `rounded-xl`, `border-border` to `border-border/50` (subtler), remove `hover:shadow-sm`, add `shadow-card` (resting shadow), `hover:shadow-card-hover` (elevated shadow), `hover:-translate-y-0.5` (lift on hover), `transition-all` instead of `transition-shadow`.

**tag.tsx** — replace the `<span>` className from:
```
inline-flex items-center rounded-full border border-border bg-surface-subtle px-md py-sm text-label text-text-secondary
```
to:
```
inline-flex items-center rounded-full border border-accent/20 bg-accent/5 px-md py-sm text-label text-accent font-medium
```
Changes: `border-border` to `border-accent/20`, `bg-surface-subtle` to `bg-accent/5`, `text-text-secondary` to `text-accent`, add `font-medium`.

Do NOT change component structure, imports, props, or any logic.
  </action>
  <verify>
    <automated>grep -q "shadow-card hover:shadow-card-hover" src/components/project-card.tsx && grep -q "border-accent/20 bg-accent/5" src/components/tag.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>Project cards have shadow-card at rest, shadow-card-hover + -translate-y-0.5 on hover, rounded-xl, border-border/50. Tags have accent tint (bg-accent/5, border-accent/20, text-accent, font-medium).</acceptance_criteria>
  <done>Project cards have depth with layered shadows and hover lift. Tags use accent color palette instead of neutral gray.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — purely visual CSS class changes on existing server components.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | I (Information Disclosure) | project-card.tsx | accept | No data changes — visual styling only |
</threat_model>

<verification>
1. `npm run build` completes without errors
2. Visual: Career timeline shows blue accent line and dots with ring glow
3. Visual: Project cards have visible shadow, lift on hover
4. Visual: Tags are blue-tinted instead of gray
</verification>

<success_criteria>
- Career timeline line is accent gradient, dots are filled accent with ring
- Project cards have shadow-card resting, shadow-card-hover + translate on hover
- Tags use accent/5 background, accent/20 border, accent text color
- No structural or functional changes to any component
</success_criteria>

<output>
After completion, create `.planning/phases/05-enterprise-ui-redesign/05-02-SUMMARY.md`
</output>
